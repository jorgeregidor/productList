<%= form_for [:admin, currency] do |form| %>
  <% if currency.errors.any? %>
    <div id="error_explanation red-text">
      <h4><%= pluralize(currency.errors.count, "error") %> prohibited this currency from being saved:</h4>

      <ul>
      <% currency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field col s12">
    <%= form.label :name %>
    <%= form.text_field :name, id: :currency_name %>
  </div>

  <div class="input-field col s12">
    <%= form.label :code %>
    <%= form.text_field :code, id: :currency_code %>
  </div>

  <div class="actions">
    <%= form.submit class: "waves-effect waves-light btn"  %>
  </div>
<% end %>
